{
  "name": "maps-api-backend",
  "version": "1.0.0",
  "description": "Backend API for Open Maps integration",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "killall -9 node || true && nodemon src/index.js",
    "clean": "killall -9 node || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.13.1",
    "pg-hstore": "^2.3.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
